/*
What is an Annotation in Spring?
--------------------------------
An annotation is a special marker that provides metadata (information about the code) to the Spring Framework. 
Spring uses annotations to configure and manage beans, dependencies, web requests, transactions, and more 
without requiring extensive XML configuration.
*/
